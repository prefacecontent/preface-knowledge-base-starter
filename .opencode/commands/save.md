---
description: Save the current conversation as a structured wiki note — picks the right folder, writes frontmatter, and updates index, log, and hot cache.
---
Load the save skill and run the save workflow for the current conversation.

Determine note type (session, entity, concept, synthesis), check for existing pages to avoid duplicates, write frontmatter with proper fields, file in the correct wiki folder, and update wiki/index.md, wiki/log.md, and wiki/hot.md.
