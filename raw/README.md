# raw

Drop your source material here: PDFs, articles, notes, transcripts, exports.

These files are immutable inputs. The agent reads them and never changes them.
Run the `wiki-ingest` skill to turn what is in here into linked wiki pages.
